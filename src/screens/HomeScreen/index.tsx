import { Button } from "react-native";

 const HomeScreen = () => {
    return (
      <Button
        title="Go to Jane's profile"
        onPress={() =>
        {}}
      />
    );
  };

  export default HomeScreen;